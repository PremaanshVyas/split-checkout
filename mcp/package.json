{
  "name": "split-checkout-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MCP server: lets an AI agent complete split-card purchases on the split-checkout demo (Airwallex sandbox, test cards only)",
  "bin": {
    "split-checkout-mcp": "./server.mjs"
  },
  "scripts": {
    "start": "node server.mjs",
    "typecheck": "echo 'plain JS workspace, no typecheck'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
